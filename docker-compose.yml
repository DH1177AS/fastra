services:
  redis:
    image: redis:7-alpine
    container_name: fastra-redis
    restart: unless-stopped
    command: ["redis-server", "--requirepass", "fastra_redis_dev"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
